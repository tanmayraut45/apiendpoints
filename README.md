Just a quick page I threw together to fetch cute dog pics.

It uses the Dog API (https://dog.ceo/dog-api/).

## To run it:

Just double click `index.html`.
Or if you want to use a server: `python3 -m http.server`
